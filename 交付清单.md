# 选题 4 交付清单

## 最终报告

- `report/自动驾驶VLA自进化研究报告.docx`：可编辑 Word 报告，含自动目录、实验表、参考文献和验收矩阵。
- `report/自动驾驶VLA自进化研究报告.pdf`：提交版 PDF，已逐页渲染检查。
- `report/项目报告.md`：报告可追踪源稿。

## 代码与数据

- `src/selfevolve_drive/`：Scenario/Trajectory schema、基座适配器、模拟器、Planner、三类 Critic、Reflection、训练、评估、Demo 服务。
- `scripts/generate_data.py`：Trajectory→Critic→Reflection 数据生成与过滤。
- `scripts/train.py`：SFT、Reflection SFT、DPO。
- `scripts/evaluate.py`：未见场景策略指标与 Critic 统计。
- `scripts/run_all.py`：数据、训练、评估一键复现。
- `data/reflection_dataset.jsonl`：实际 5,000 条记录。
- `models/`：SFT、Reflection SFT、Reflection DPO 和 Reward Model 参数。
- `outputs/metrics.json`：实际全量运行结果。
- `configs/default.json`：固定随机种子与实验参数。

## Demo 与测试

- `scripts/run_demo.py` + `demo/index.html`：本地动态双轨迹驾驶评审台 Demo。
- `启动Demo.bat`：Windows 双击启动入口。
- `docs/Demo使用说明.md`：现场操作、界面含义和故障排查。
- `docs/基座模型与轻量化说明.md`：OpenDriveVLA-0.5B 选择依据、真实性边界和 LoRA 方案。
- `scripts/download_base_model.py`：授权后下载必要 OpenDriveVLA-0.5B 基座到本目录。
- `references/models/OpenDriveVLA-0.5B/README_DOWNLOAD.md`：官方模型网址、gated 访问状态和下载步骤。
- `tests/test_core.py`：生成确定性、红灯反思、流水线、基座披露和双策略接口测试。

## 复现命令

```powershell
python -m pip install -r requirements.txt
python scripts/run_all.py --samples 5000
python -m unittest discover -s tests -v
python scripts/run_demo.py
```

打开 `http://127.0.0.1:8000`。

## 已核验结果

- 数据行数：5,000；未见测试场景：1,000；质量过滤后保留：2,730。
- Baseline → Reflection SFT：违章代理率 45.4% → 7.2%，整体得分 91.176 → 97.920。
- Reflection DPO：违章代理率 5.1%，行人礼让失败率 0.3%。
- 单元测试、Demo 双策略 API 冒烟测试与 Word/PDF 逐页检查均通过。

## 范围声明

默认实验是 CPU 可复现的合成开放环代理，不宣称为 nuScenes/CARLA 闭环成绩。完整模型实验必须使用 OpenDriveVLA-0.5B 预训练基座和 LoRA，不从零训练；当前 Demo 明确未加载其权重。官方模型为 gated repository，当前无用户 `HF_TOKEN`，已交付官方地址与下载脚本，授权后下载到本目录。
